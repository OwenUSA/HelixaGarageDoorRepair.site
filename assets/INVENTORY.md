# assets/INVENTORY.md — asset slots, provenance, status

Generated by `../_shared/harness/src/inventory.mjs` from `.harness/assets/*.json` (rendered
geometry, four widths) and `.harness/cap/ref/**/sec-*.png` (dominant colour). Machine
readable: `.harness/inventory.json`. **Regenerate, never hand-edit the tables** — narrative
lives in `assets/INVENTORY.head.md` and `assets/INVENTORY.tail.md`, which are sandwiched
around the generated block.

## Provenance rule (D-09), stated once

| verdict | meaning |
|---|---|
| **TAKE** | generic UI iconography and open-licensed fonts, verifiable in one step. Listed in the tail section — they have no reference *slot*, so they are not rows in the geometry table. |
| **REPLACE** | belongs to the reference business — photographs, wordmark, badges, signage. **Never downloaded into this repo.** A placeholder SVG at the exact rendered geometry ships in `public/placeholders/` until the Prompt 11 hand-back (OVERRIDE 3, floor F-05). |
| **DELETED** | inventoried and deliberately not filled: the D-02 locations tree, the D-13 review furniture, the D-15 cookie bar, the lightbox twins, and the six raster service icons we replace with `lucide-react`. |

**Zero REPLACE assets were downloaded.** The 180-file, 16 MB mirror under
`reference/assets/` is a *measurement artefact* used only to make the saved reference render
correctly (see `docs/profile.md`); it is gitignored, it never enters `public/`, and no file
in it is ever served by our build.

## How the geometry was taken

`assets.mjs` walks `<img>` (including `currentSrc` after `srcset` resolution), `<source>`,
CSS `url()` on **every** element, `<link rel=preload>`, `@font-face` sources and
video/poster, at 390 / 430 / 768 / 1440 against the locally-served reference. The
"highest resolution actually served" is the `natural` size of the `currentSrc` the browser
picked at the widest breakpoint, not the largest entry in the `srcset` attribute — those
differ wherever the theme's `sizes` attribute caps the choice.

WordPress srcset variants (`House6-300x200-1.png`, `…-980x574.png`) collapse to one slot.
The Responsive Lightbox full-size twins do **not**: they render at `0×0` and would otherwise
hand a 150×150 thumbnail slot a zero-area geometry.

## Near-white repaint

Six REPLACE slots sample near-white. The **table below keeps the honestly sampled hex**; the
generated SVG file is repainted down to a mountable mid-neutral of the same hue
(`placeholderMaxLum 0.62` → `placeholderTargetLum 0.28`). A near-white placeholder under
body copy makes `rendertruth.mjs` report `UNMEASURABLE` for that band, which is an absence
masquerading as a pass.

| slot | sampled | file painted |
|---|---|---|
| `logo-header` | `#f7f1f2` | `#8f8c8c` |
| `badge-cert-inline` | `#f6f5f5` | `#8f8e8e` |
| `badge-cert-panel` | `#f0efef` | `#908f8f` |
| `home-cta-image` | `#d7d8d4` | `#8e8f8c` |
| `services-card-image` | `#ebeae9` | `#8d8c8c` |
| `services-cta-bg` | `#dcd7d4` | `#918e8c` |


## REPLACE — every slot, with the geometry a generator needs

| slot ID | route | section | kind | 390 | 768 | 1440 | aspect | object-fit | dominant | aspect Δ | placeholder |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `about-banner-image` | /about | about-banner | img | 1443x486 | 1152x388 | 1440x485 | 2.97:1 | fill | `#1f1f1d` | no | `about-banner-image.svg` |
| `badge-cert-inline` | / | home-why | img | 300x71 | 300x71 | 300x71 | 4.23:1 | fill | `#f6f5f5` | no | `badge-cert-inline.svg` |
| `contact-banner-image` | /contact | contact-banner | img | 1170x203 | 768x133 | 1440x250 | 5.76:1 | fill | `#222323` | **yes** | `contact-banner-image.svg + contact-banner-image-alt.svg` |
| `contact-photo` | /contact | contact-main | img | 200x300 | 200x300 | 200x300 | 2:3 | fill | `#d8b9bc` | no | `contact-photo.svg` |
| `hero-image` | / | home-hero | img | 1170x463 | 768x304 | 1440x569 | 2.53:1 | fill | `#27201d` | no | `hero-image.svg` |
| `home-cta-image` | / | home-cta | img | 390x177 | 320x578 | 600x366 | 1.64:1 | fill | `#d7d8d4` | **yes** | `home-cta-image.svg + home-cta-image-alt.svg` |
| `services-banner-image` | /services | services-banner | img | 1170x203 | 768x133 | 1440x250 | 5.76:1 | fill | `#30272d` | **yes** | `services-banner-image.svg + services-banner-image-alt.svg` |
| `services-card-image` | /services | services-grid | img | 366x244 | 296x197 | 306x204 | 3:2 | fill | `#ebeae9` | **yes** | `services-card-image.svg + services-card-image-alt.svg` |
| `services-cta-bg` | /services | services-cta | bg | 390x654 | 768x539 | 1440x392 | 3.67:1 | cover | `#dcd7d4` | **yes** | `services-cta-bg.svg + services-cta-bg-alt.svg` |
| `services-thumb-1` | /services | services-intro | img | 150x150 | 150x150 | 150x150 | 1:1 | fill | `#d3bdbf` | no | `services-thumb-1.svg` |
| `services-thumb-2` | /services | services-intro | img | 150x150 | 150x150 | 150x150 | 1:1 | fill | `#d3bdbf` | no | `services-thumb-2.svg` |
| `services-thumb-3` | /services | services-intro | img | 150x150 | 150x150 | 150x150 | 1:1 | fill | `#d3bdbf` | no | `services-thumb-3.svg` |
| `services-thumb-4` | /services | services-intro | img | 150x150 | 150x150 | 150x150 | 1:1 | fill | `#d3bdbf` | no | `services-thumb-4.svg` |
| `services-thumb-5` | /services | services-intro | img | 150x150 | 150x150 | 150x150 | 1:1 | fill | `#d3bdbf` | no | `services-thumb-5.svg` |
| `badge-cert-panel` | all | about-story / contact-main | img | 300x124 | 300x124 | 300x124 | 2.42:1 | fill | `#f0efef` | no | `badge-cert-panel.svg` |
| `logo-footer` | all | site-footer | img | 200x101 | 200x101 | 200x101 | 1.98:1 | fill | `#343031` | no | `logo-footer.svg` |
| `logo-header` | all | site-header | img | 175x88 | 175x88 | 175x88 | 1.99:1 | fill | `#f7f1f2` | no | `logo-header.svg` |

## DELETED — inventoried, deliberately not filled

| slot ID | route | section | 1440 | why |
|---|---|---|---|---|
| `badge-5-stars` | / | home-reviews | 179x40 | Five-star graphic. D-13 — no invented rating; the band ships [TESTIMONIAL PLACEHOLDER] blocks. |
| `badge-google-reviews` | / | home-reviews | 197x100 | Google Reviews lockup. D-13 — no review count, no third-party review branding. |
| `city-card-photo` | / | home-locations-grid / about-locations-grid | 720x481 | City-card photograph in the Meet Our Teams grid. Whole band deleted per D-02. |
| `cookie-bar-logo` | / | — | — | GDPR Cookie Compliance plugin logo. We ship no cookie bar (D-15). |
| `icon-service-card` | / | home-services | 197x180 | Six bespoke raster service icons. We ship `lucide-react` line icons instead — no raster asset, no placeholder. |
| `lightbox-fullsize-twin` | /services | services-intro | 150x150 | Responsive Lightbox full-size twin of a thumbnail; renders 0x0. We ship no lightbox. |

## Tally

| | count |
|---|---|
| slots inventoried | 23 |
| REPLACE | 17 |
| DELETED | 6 |
| placeholders generated | 22 |
| REPLACE assets downloaded | **0** |


---

## TAKE — the only things actually acquired

No reference *file* is taken. What is taken is open-licensed and fetched from its own
upstream, never from the reference's CDN.

| item | source | licence | verified in one step | used for |
|---|---|---|---|---|
| **Raleway** (700, 800) | `next/font/google` | SIL OFL 1.1 | Google Fonts licence page states OFL | headings — the reference's own heading face, so heading metrics **converge**; not a floor |
| **Source Sans 3** (400, 600, 700) | `next/font/google` | SIL OFL 1.1 | Google Fonts licence page states OFL | body copy — substitute for the reference's commercial `ff-tisa-sans-web-pro` per D-11. **Permanent floor F-02** |
| **lucide-react** | npm, allowlisted | ISC | package `license` field | every icon on the site |

**Not taken, and why each looked like it should be:**

- `FontAwesome` kit `080b110fc3` — the reference's icon font, delivered by an Adobe Fonts
  loader. Replaced wholesale by `lucide-react`. No icon font ships.
- `Nunito` — has two real `@font-face` rules, which makes it look like a legitimate
  substitution floor. It paints exactly four elements, all inside the GDPR cookie bar we do
  not ship (D-15). Booking it would permanently excuse text that should converge.
- `gform-icons-theme` — one real `@font-face` rule, never loads, zero usages. Phantom.
- `ff-tisa-sans-web-pro` and `raleway` have **no `@font-face` rule anywhere in the reference
  CSS** and yet paint the whole page — injected at runtime by the kit script.
  `document.fonts.check()` returns `true` for families that do not exist, so it settles
  nothing; canvas advance-width measurement against a known-bogus family settled it. Full
  working in `docs/profile.md`.

## Logo

There is no logo file and none is invented.

| | |
|---|---|
| header slot | `logo-header`, 175×88 at all three breakpoints, `object-fit: contain` |
| footer slot | `logo-footer`, 200×101 at all three breakpoints, reversed |
| what ships now | a **wordmark set in the display font** (Raleway 800) reading *Helixa Garage Door Repair*, plus a placeholder SVG at slot geometry |
| what is missing | `TODO(fact): logo asset` — FN-13 in `docs/facts-needed.md` |
| prompt | written in `docs/asset-prompts.md` at Prompt 11 (OVERRIDE 2): wordmark + icon lockup, display font and applied palette hues named explicitly |

## Badges — removed, not filled

`badge-cert-inline` (`home-why`) and `badge-cert-panel` (`about-story`) were
manufacturer-certification lockups on the reference. Rather than hold the slot as a
`TODO(fact):` chip (FN-04, D-14), the slots have been removed outright — no certification
we can evidence, and no placeholder standing in for one. `badge-cert-inline.svg` and
`badge-cert-panel.svg` are deleted from `public/placeholders/`; the rows above are kept as
a historical record of the original build.
No certification, licence number, bond, insurance status, BBB rating, founding year or team
size is invented anywhere on this site.

## Video

None. The reference ships no `<video>`, no `<source type="video/*">` and no poster image on
any of its four pages.

## Status

Every REPLACE slot is at **placeholder** until the terminal hand-back (OVERRIDE 3). That is
floor **F-05** in `docs/known-divergence.md`, not a defect, and no iteration is spent on it.
